from django.urls import path, include
from rest_framework.routers import DefaultRouter

from LittleLemonAPI import views


# router = DefaultRouter(trailing_slash=False)
# router.register(r'menu-items', views.MenuItemViewSet, namespace='menu-items')
# router.register('categories', views.CategoryViewSet, basename='categories')
# router.register('managers', views.ManagerViewSet, basename='managers')
# router.register('delivery-crew', views.DeliveryCrewViewSet, basename='delivery-crew')
# router.register('orders', views.OrderViewSet, basename='orders')
# router.register('cart', views.CartViewSet)

# app_name = 'LittleLemonAPI'

# urlpatterns = [
# 	path('', include(router.urls)),
	
# ]

